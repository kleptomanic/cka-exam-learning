Restore the etcd restore where the backup is located at /opt/etcd_backup/ named as etcd_backup_<date>.db into the data directory /var/lib/etcd_backed. Make sure etcd container is running and functioning well. Write the ncecessary commands into /opt/etcd/etcd_restore.sh.