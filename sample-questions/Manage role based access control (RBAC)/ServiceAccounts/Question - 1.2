Create a Pod named frontend-pod using the image nginx that should run using deploy-user service account. Exec into pod, and write the token used by pod for deploy-user service account into /opt/rbac/pod_token_details.txt. 


Now, Create the same pod but deny the automatically mounting of the token in service Account.