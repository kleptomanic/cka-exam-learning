Configure a User named kube-pod-admin which has a cluster wide permission to view, create, update and delete all the pods in that specific cluster. Also create a context named as cluster-pod-admin which should be mapped with the relevant user. Create a pod name pod-by-admin with image nginx.